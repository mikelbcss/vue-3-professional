```vue
<template>
  <div class="p-6 max-w-md mx-auto bg-white rounded-xl shadow-lg">
    <h2 class="text-2xl font-bold mb-6">{{ title }}</h2>

    <!-- Cart Items -->
    <div class="mb-6">
      <h3 class="text-lg font-semibold mb-3">Productos en el carrito:</h3>
      <div v-for="item in items" :key="item.id" class="flex justify-between items-center p-3 bg-gray-50 rounded mb-2">
        <div>
          <span class="font-medium">{{ item.name }}</span>
          <span class="text-gray-500"> - €{{ item.price }}</span>
        </div>
        <div class="flex items-center space-x-2">
          <button @click="decrementQuantity(item.id)" class="px-2 py-1 bg-red-500 text-white rounded text-sm">-</button>
          <span class="mx-2">{{ item.quantity }}</span>
          <button @click="incrementQuantity(item.id)" class="px-2 py-1 bg-green-500 text-white rounded text-sm">
            +
          </button>
          <button @click="removeItem(item.id)" class="px-2 py-1 bg-gray-500 text-white rounded text-sm ml-2">
            Eliminar
          </button>
        </div>
      </div>
    </div>

    <!-- Add New Item -->
    <div class="mb-6 p-4 bg-blue-50 rounded">
      <h3 class="font-semibold mb-2">Agregar producto:</h3>
      <div class="flex space-x-2">
        <input v-model="newItemName" placeholder="Nombre" class="flex-1 px-2 py-1 border rounded" />
        <input v-model="newItemPrice" type="number" placeholder="Precio" class="w-20 px-2 py-1 border rounded" />
        <button @click="addItem" class="px-3 py-1 bg-blue-500 text-white rounded">+</button>
      </div>
    </div>

    <!-- Cart Statistics (Automatically Computed) -->
    <div class="bg-gray-100 p-4 rounded">
      <h3 class="font-semibold mb-2">Resumen del carrito:</h3>
      <p><strong>Total de productos:</strong> {{ totalItems }}</p>
      <p><strong>Total de artículos únicos:</strong> {{ uniqueItemsCount }}</p>
      <p><strong>Precio total:</strong> €{{ totalPrice.toFixed(2) }}</p>
      <p><strong>Precio promedio:</strong> €{{ averagePrice.toFixed(2) }}</p>
      <p v-if="hasExpensiveItems" class="text-orange-600"><strong>⚠️ Tienes productos caros (+€50)</strong></p>
      <p><strong>Estado del carrito:</strong> {{ cartStatus }}</p>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'

// Reactive data
const title = ref('Carrito de Compras - Con Computed Properties')
const items = ref([
  { id: 1, name: 'Laptop', price: 999, quantity: 1 },
  { id: 2, name: 'Mouse', price: 25, quantity: 2 },
  { id: 3, name: 'Teclado', price: 75, quantity: 1 },
])

const newItemName = ref('')
const newItemPrice = ref(0)

// ✅ SOLUCIÓN: Estado derivado automáticamente con computed()
const totalItems = computed(() => {
  return items.value.reduce((sum, item) => sum + item.quantity, 0)
})

const uniqueItemsCount = computed(() => {
  return items.value.length
})

const totalPrice = computed(() => {
  return items.value.reduce((sum, item) => sum + item.price * item.quantity, 0)
})

const averagePrice = computed(() => {
  return uniqueItemsCount.value > 0 ? totalPrice.value / uniqueItemsCount.value : 0
})

const hasExpensiveItems = computed(() => {
  return items.value.some(item => item.price > 50)
})

const cartStatus = computed(() => {
  if (items.value.length === 0) {
    return 'Carrito vacío'
  } else if (totalPrice.value > 500) {
    return 'Carrito premium'
  } else {
    return 'Carrito con productos'
  }
})

// ✅ Funciones simplificadas: Sin sincronización manual
const incrementQuantity = (id: number) => {
  const item = items.value.find(item => item.id === id)
  if (item) {
    item.quantity++
    // ✅ No necesita updateCartStatistics() - se actualiza automáticamente
  }
}

const decrementQuantity = (id: number) => {
  const item = items.value.find(item => item.id === id)
  if (item && item.quantity > 1) {
    item.quantity--
    // ✅ No necesita updateCartStatistics() - se actualiza automáticamente
  }
}

const removeItem = (id: number) => {
  items.value = items.value.filter(item => item.id !== id)
  // ✅ No necesita updateCartStatistics() - se actualiza automáticamente
}

const addItem = () => {
  if (newItemName.value && newItemPrice.value > 0) {
    const newId = Math.max(...items.value.map(item => item.id)) + 1
    items.value.push({
      id: newId,
      name: newItemName.value,
      price: newItemPrice.value,
      quantity: 1,
    })
    newItemName.value = ''
    newItemPrice.value = 0
    // ✅ No necesita updateCartStatistics() - se actualiza automáticamente
  }
}
</script>
```

## 🎯 Explicación de la Solución

### Cambios Principales

#### 1. **Import de `computed`**

```js
import { ref, computed } from 'vue'
```

#### 2. **Conversión de refs manuales a computed properties**

**Antes (problemático):**

```js
const totalItems = ref(4) // Valor hardcodeado
const updateCartStatistics = () => {
  totalItems.value = items.value.reduce((sum, item) => sum + item.quantity, 0)
}
```

**Después (automático):**

```js
const totalItems = computed(() => {
  return items.value.reduce((sum, item) => sum + item.quantity, 0)
})
```

#### 3. **Eliminación completa de sincronización manual**

- ❌ Eliminada función `updateCartStatistics()`
- ❌ Eliminadas todas las llamadas manuales de actualización
- ✅ Todo se actualiza automáticamente cuando `items` cambia

### Computed Properties Implementados

#### **`totalItems`** - Total de productos

```js
const totalItems = computed(() => {
  return items.value.reduce((sum, item) => sum + item.quantity, 0)
})
```

#### **`uniqueItemsCount`** - Artículos únicos

```js
const uniqueItemsCount = computed(() => {
  return items.value.length
})
```

#### **`totalPrice`** - Precio total

```js
const totalPrice = computed(() => {
  return items.value.reduce((sum, item) => sum + item.price * item.quantity, 0)
})
```

#### **`averagePrice`** - Precio promedio

```js
const averagePrice = computed(() => {
  return uniqueItemsCount.value > 0 ? totalPrice.value / uniqueItemsCount.value : 0
})
```

_Nota: Usa otros computed properties y previene división por cero_

#### **`hasExpensiveItems`** - Productos caros

```js
const hasExpensiveItems = computed(() => {
  return items.value.some(item => item.price > 50)
})
```

#### **`cartStatus`** - Estado del carrito

```js
const cartStatus = computed(() => {
  if (items.value.length === 0) {
    return 'Carrito vacío'
  } else if (totalPrice.value > 500) {
    return 'Carrito premium'
  } else {
    return 'Carrito con productos'
  }
})
```

_Nota: Depende de múltiples computed properties_

## ✨ Beneficios Obtenidos

### **Automático y Reactivo**

- Las estadísticas se actualizan instantáneamente al cambiar items
- No hay posibilidad de olvidar actualizar el estado
- Eliminado código repetitivo de sincronización

### **Performance Optimizada**

- Vue cachea los resultados de computed properties
- Solo recalcula cuando cambian las dependencias
- Evita cálculos innecesarios

### **Código Más Limpio**

- **-30 líneas** de código eliminadas (función updateCartStatistics + llamadas)
- Funciones más simples y enfocadas
- Lógica declarativa en lugar de imperativa

### **Mantenible y Confiable**

- Un solo lugar donde se define cada cálculo
- Imposible que el estado quede desincronizado
- Fácil agregar nuevas estadísticas computadas

## 🔍 Conceptos Avanzados

### **Computed Dependencies Chain**

```js
// uniqueItemsCount depende de items
const uniqueItemsCount = computed(() => items.value.length)

// totalPrice depende de items
const totalPrice = computed(() => /* ... */)

// averagePrice depende de uniqueItemsCount Y totalPrice
const averagePrice = computed(() =>
  uniqueItemsCount.value > 0 ? totalPrice.value / uniqueItemsCount.value : 0
)
```

Vue rastrea automáticamente toda la cadena de dependencias.

### **Lazy Evaluation**

Los computed properties solo se ejecutan cuando:

1. Se accede a su valor
2. Sus dependencias han cambiado

### **Template Usage**

En el template, los computed se usan exactamente igual que refs:

```vue
<p>Total: {{ totalItems }}</p>
<p>Precio: €{{ totalPrice.toFixed(2) }}</p>
```

## 🚀 Resultado Final

El carrito ahora:

- ✅ Se actualiza automáticamente
- ✅ Tiene mejor performance
- ✅ Es más fácil de mantener
- ✅ Está libre de bugs de sincronización
- ✅ Usa las mejores prácticas de Vue 3
